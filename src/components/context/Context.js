import { createContext } from "react";

export const SideMenuContext = createContext('')
export const WorkTaskContext = createContext([])
export const PersonalTaskContext = createContext([])
export const GamesTaskContext = createContext([])
export const totalTaskContext = createContext([])
export const rightSettingContext = createContext([])